import React from "react";

const Header = (props) => {
    return (
        <header className="App-header">
            <p>
                IT Rex test
            </p>

        </header>
    )
};

export default Header;